overwrite: true
schema: "http://localhost:4000/ms/impact/graphql"
documents: null
generates:
  ../shared/src/generated_graphql_types.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
